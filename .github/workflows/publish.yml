# Generated by npm-dual-publish. Edit the template, not this file.
name: Publish to npm

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  packages: read
  id-token: write

jobs:
  publish:
    uses: intisy/workflows/.github/workflows/publish-npm.yml@main
    with:
      unscoped_name: 'openai-translator'
      patch_ceiling: 20
      needs_java: true
