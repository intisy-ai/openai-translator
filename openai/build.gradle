// OpenAI translator + codecs. Zero-dependency, Java-8-clean (TeaVM-transpilable), mirroring
// basekit's :ir. Depends on :ir for the Translator interface + IR types.
dependencies {
    githubImplementation "intisy-ai:basekit:4.2.0:ir"
    githubTestImplementation "intisy-ai:basekit:4.2.0:ir-test-fixtures"
}
